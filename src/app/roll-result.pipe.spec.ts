import { RollResultPipe } from './roll-result.pipe';

describe('RollResultPipe', () => {
  it('create an instance', () => {
    const pipe = new RollResultPipe();
    expect(pipe).toBeTruthy();
  });
});
